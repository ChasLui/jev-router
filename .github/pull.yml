version: "1"
rules:
  # Merge upstream instead of the pull app's default hardreset, which wiped
  # PR #1-#3 from master on 2026-09-20.
  - base: master
    upstream: gargpratyush:master
    mergeMethod: merge
    mergeUnstable: false
